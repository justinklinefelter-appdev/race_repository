<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Comments

    <a data-toggle="collapse" href="#user_race_comments_new_form">
      Add comment
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @user_race.comments.each do |comment| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/comments/<%= comment.id %>">
          <%= comment.body %>
        </a>

        <%if current_user == comment.commentor %>
          <a href="/delete_comment_from_user_race/<%= comment.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="user_race_comments_new_form">
        <%= render partial: "user_race_templates/user_race_comments_new_form" %>
      </div>
    </div>

  </ul>
</div>
